// Minimal preview to avoid "Cannot read properties of undefined (reading 'prototype')"
// (often caused by legacy parameters.options or addon conflicts in SB7)
module.exports = {
  parameters: {},
};
