import { iPhone, iPod, iPad } from '../utils/device-detection';

export default function({ eventEmitter, engine, ui }) {
  const engineNode = engine.getNode();
  
  if (iPhone || iPod || iPad) {

  }
}
